// Node.js SSR entry (Plesk deployment).
// On the node-server preset, Nitro wraps TanStack's server-entry into a
// standard Node HTTP server. We just re-export it.
export { default } from "@tanstack/react-start/server-entry";
